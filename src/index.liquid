---
layout: base.liquid
title: Discoliam
body-class: home
---

<p class="lead"><span>Discoliam</span> (Liam Edward Richardson to his parents) is a <span>User Interface Developer</span> 🤓 currently based in <span>Vancouver, BC</span> 🇨🇦. For {% yearDiff 2010 %} years he has built award winning <span>Websites, Applications and Installations</span> for clients around the globe 🌎. Outside of work, he enjoys live music 🎸, rides bicycles 🚲 and eats pizza 🍕.</p>

<section class="projects">
<h2>Potted History of Projects</h2>
<ul>
  {%- for project in projects -%}
    <li>
      <span class="sr-only">{{ project }}</span>
      <svg class="icon svg-{{project | downcase | replace: ' ', '-' }}"><use xlink:href="#{{project | downcase | replace: ' ', '-' }}"></use></svg>
    </li>
  {%- endfor -%}
</ul>
</section>

<section class="previous-clients">
  <h2>Previous Clients</h2>
  <ul>
    {%- for client in clients -%}
      <li>
        <span class="sr-only">{{ client }}</span>
        <svg class="icon svg-{{client | downcase | replace: ' ', '-' }}"><use xlink:href="#{{client | downcase | replace: ' ', '-' }}"></use></svg>
      </li>
    {%- endfor -%}
  </ul>
</section>